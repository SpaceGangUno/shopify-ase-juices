{{ 'section-benefits.css' | asset_url | stylesheet_tag }}

<div class="page-width">
  <div class="benefits-preview">
    <h2>{{ section.settings.heading }}</h2>
    <div class="benefits-grid">
      {% for block in section.blocks %}
        <div class="benefit-card" {{ block.shopify_attributes }}>
          <div class="benefit-icon">{{ block.settings.icon }}</div>
          <h3>{{ block.settings.title }}</h3>
          <p>{{ block.settings.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Benefits Section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Why Choose Asé Juices?"
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (emoji)",
          "default": "🌿"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "100% Organic"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "All our ingredients are certified organic and locally sourced when possible."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Benefits Section",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "icon": "🌿",
            "title": "100% Organic",
            "description": "All our ingredients are certified organic and locally sourced when possible."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "⚡",
            "title": "Natural Energy",
            "description": "Experience sustained energy without crashes or artificial stimulants."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "🔄",
            "title": "Fresh Pressed Daily",
            "description": "Every bottle is pressed fresh daily to ensure maximum nutrition and taste."
          }
        }
      ]
    }
  ]
}
{% endschema %}
